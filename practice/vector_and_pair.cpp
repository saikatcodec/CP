#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<int> v;
    for (int i = 0; i < 5; i++)
    {
        cin >> v[i];
    }

    for (auto ele : v)
        cout << ele << "\t";

    return 0;
}